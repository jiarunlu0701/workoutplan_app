import XCTest
@testable import MinimalAnimation_2

final class MinimalAnimation_2Tests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MinimalAnimation_2().text, "Hello, World!")
    }
}
